export class Truck {
  constructor(public name: string, public location: string, public cuisine: string, public owner: string, public phone: string, public website: string, public awards: string, public id: number) {}
}
